<category>
    <id>{{category.id}}</id>
    <name>{{category.name}}</name>
    <description>{{category.description}}</description>
    <items>
    {% for i in category.items %}
        <item>
            <id>{{i.id}}</id>
            <name>{{i.name}}</name>
            <description>{{i.description}}</description>
            <image>{{url_for('item_view', category_name=c.name, item_name=i.name)}}</image>
        </item>
    {% endfor %}
    </items>
</category>